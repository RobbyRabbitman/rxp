import { UiMarbleDirective } from './ui-marble.directive';

describe('UiMarbleDirective', () => {
  it('should create an instance', () => {
    const directive = new UiMarbleDirective();
    expect(directive).toBeTruthy();
  });
});
